# Provisional pre-conformance-fix outputs

This directory preserves the original A1–A4 run before the frozen-plan conformance
bugfix. Its A3/A4 merge results are not authoritative because
`hard_limit_fallback` boundaries were incorrectly eligible and proposal ranking used
relative pair shift instead of absolute cohesion margin.

Authoritative post-fix outputs are under `../v4-ablation/`.
