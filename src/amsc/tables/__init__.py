"""A table, as a retriever indexes it and as a reader sees it.

:mod:`~amsc.tables.search_text` produces the text a lexical index should hold
for a table; :mod:`~amsc.tables.view` produces the text a reader (or a model)
should be shown, including ``CONTEXT_HEADER``. Two audiences, two renderings,
one source row.
"""
