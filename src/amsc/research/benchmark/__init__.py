"""The frozen benchmarks: three-arm chunker comparison, retrieval, holdout.

Each writes a directory of results whose numbers are pinned by
``tests/integration``. :mod:`~amsc.research.benchmark.run_retrieval` and
:mod:`~amsc.research.benchmark.run_holdout` are the ``python -m`` runners.
"""
