"""The v1 Agentic arm: a per-boundary SPLIT/KEEP judge and the chunker over it.

Superseded by Deep Analysis, which inverted the arrangement (see
:mod:`amsc.deep.selector` for the audit that motivated it). Kept because it is
one of the benchmark compared arms and its result is part of the record.
"""
