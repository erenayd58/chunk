"""The Viewer demo question-answering stack.

Index, retrieve, assemble context, answer. It exists so the Viewer can
demonstrate what a chunking does to a real question; the console has its own
pipeline and must not import this one.
"""
